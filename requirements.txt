requests>=2.31
feedparser>=6.0
python-dotenv>=1.0
beautifulsoup4>=4.12
numpy>=1.24
sentence-transformers>=2.7
praw>=7.7  # only for standalone_reddit_digest.py
telethon>=1.34  # only for standalone_telegram_digest.py
pyyaml>=6.0  # x_sources.yaml for the X acquisition cascade (x_acquire.py)
# Optional X providers (install only if you enable that layer in .env):
#   twscrape>=0.13   # L3 session scraper  (X_TWSCRAPE_ENABLED=1)
#   playwright>=1.44  # L4 logged-in browser (X_BROWSER_STATE=...)
